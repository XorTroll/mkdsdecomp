#include <ntr/os/os_misc.h>

void Os_ReferSymbol(const char *symbol) {
    // Nothing is done here
    // This is probably just used to force symbol strings to appear in the final game, or is just a debug functionality
}
