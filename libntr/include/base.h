#ifndef _NTR_BASE_H
#define _NTR_BASE_H

#include <stdlib.h>
#include <stdint.h>
#include <stdio.h>
#include <stdbool.h>

typedef uint32_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
typedef uint64_t u64;

#define NTR_BIT(n) (1 << (n))

#endif